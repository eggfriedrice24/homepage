// `ViewTransition` ships in React's canary/experimental channel, which Next
// swaps in when experimental.viewTransition is enabled. The stable
// @types/react entrypoint doesn't declare it, so pull in the canary types.
/// <reference types="react/canary" />
